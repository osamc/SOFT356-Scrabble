var assert = require('chai').assert;
var expect = require('chai').expect;

describe('Game Test', () => {

    it('dummy test', (done) => {
        console.log('dummy');
        expect(2).equal(2);
        done();
    })

})
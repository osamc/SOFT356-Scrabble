var assert = require('chai').assert;
var expect = require('chai').expect;

describe('Game Test', () => {

    it('should do something', (done) => {
        expect(2).equal(2);
        done();
    })

})